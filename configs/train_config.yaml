# DAWN Training Configuration
# Dynamic Neuron Transformer Architecture

# Data
data:
  base_dir: /content/drive/MyDrive/data
  train_file: train/wikitext_5to1_texts.pkl
  val_file: validation/wikitext_5to1_texts.pkl

# Model Architecture (Dynamic Neuron Transformer)
model:
  d_model: 256            # Hidden dimension (reduced for efficiency)
  n_layers: 4             # Number of transformer layers
  n_heads: 4              # Number of attention heads
  n_neurons: 256          # Size of neuron pool per layer
  n_patterns: 128         # Size of pattern pool per layer
  k: 8                    # Top-k neurons to select
  pattern_k: 16           # Top-k patterns to select (prevents collapse)
  d_rank: 64              # Low-rank dimension for forced diversity (v3.2)
  d_ff: 512               # Feed-forward dimension (2x d_model)
  max_seq_len: 128        # Maximum sequence length
  dropout: 0.1            # Dropout rate

# Training
training:
  batch_size: 128         # Batch size
  num_epochs: 30          # Total epochs
  lr: 0.0003              # Learning rate (increased for faster convergence)
  weight_decay: 0.1       # Weight decay for regularization
  warmup_epochs: 2        # Warmup epochs

# Mixed precision (faster training on GPU)
use_amp: true

# Paths
checkpoint_dir: /content/drive/MyDrive/dawn/checkpoints
log_dir: /content/drive/MyDrive/dawn/checkpoints
