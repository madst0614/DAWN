# DAWN v7.7 Training Config
# QK/VO Basis Separation with Symmetric O Projection

# Data
data:
  base_dir: /content/drive/MyDrive/data
  train_file: train/wikitext_5to1_texts.pkl
  val_file: validation/wikitext_5to1_texts.pkl

# Model Architecture (v7.7: QK/VO Basis Separation)
model:
  model_version: "7.7"    # DAWN v7.7 - QK/VO Basis Separation
  d_model: 256            # Hidden dimension
  n_layers: 4             # Number of transformer layers
  n_heads: 4              # Number of attention heads

  # Neuron routing
  n_neurons: 128          # Size of neuron pool per layer
  neuron_k: 8             # Top-k neurons to select

  # Basis parameters (v7.7: basis_qk + basis_vo, O uses basis_vo.T)
  n_basis: 32             # Number of basis filters (shared across layers)
  basis_rank: 64          # Rank for Q/K/V (4 heads x 16 d_head)

  # Architecture
  d_ff: 1024              # Feed-forward dimension (Standard FFN)
  max_seq_len: 128        # Maximum sequence length
  dropout: 0.1            # Dropout rate

# Training
training:
  batch_size: 128         # Batch size
  num_epochs: 30          # Total epochs
  lr: 0.0003              # Learning rate
  weight_decay: 0.1       # Weight decay for regularization
  warmup_epochs: 2        # Warmup epochs

  # Regularization (v7.7: orthogonality + diversity + load balance)
  orthogonality_weight: 0.01  # Basis orthogonality loss (basis_qk + basis_vo)
  diversity_weight: 0.1       # Recipe diversity loss
  load_balance_weight: 0.01   # Load balance loss for neuron routing

# Mixed precision (faster training on GPU)
use_amp: true

# Paths
checkpoint_dir: /content/drive/MyDrive/dawn/checkpoints_v77
log_dir: /content/drive/MyDrive/dawn/checkpoints_v77
