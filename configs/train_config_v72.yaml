# DAWN v7.2 Training Config
# Standard FFN + Neuron Routing (병목 탐색 실험)

# Data
data:
  base_dir: /content/drive/MyDrive/data
  train_file: train/wikitext_5to1_texts.pkl
  val_file: validation/wikitext_5to1_texts.pkl

# Model Architecture (v7.2: Standard FFN + Neuron Routing)
model:
  model_version: "7.2"    # DAWN v7.2 - Standard FFN + Neuron Routing
  d_model: 256            # Hidden dimension
  n_layers: 4             # Number of transformer layers
  n_heads: 4              # Number of attention heads

  # Neuron routing (same as v7.0/v7.1)
  n_neurons: 64           # Size of neuron pool per layer
  neuron_k: 8             # Top-k neurons to select

  # Basis parameters (ignored in v7.2, for compatibility)
  n_basis: 32             # Not used in v7.2
  basis_rank: 64          # Not used in v7.2

  # Architecture
  d_ff: 1024              # Feed-forward dimension (Standard FFN)
  max_seq_len: 128        # Maximum sequence length
  dropout: 0.1            # Dropout rate

# Training
training:
  batch_size: 128         # Batch size
  num_epochs: 30          # Total epochs
  lr: 0.0003              # Learning rate
  weight_decay: 0.1       # Weight decay for regularization
  warmup_epochs: 2        # Warmup epochs

  # Regularization (v7.2: only load balance)
  diversity_weight: 0.0      # Not applicable (no recipe)
  load_balance_weight: 0.01  # Load balance loss

# Mixed precision (faster training on GPU)
use_amp: true

# Paths
checkpoint_dir: /content/drive/MyDrive/dawn/checkpoints_v72
log_dir: /content/drive/MyDrive/dawn/checkpoints_v72
